# AI-Translator-Critic-Flask-Edition-
AI Translator & Critic 

## Настройка API ключа 🔒

Чтобы приложение работало и ваш API ключ не был раскрыт, выполните следующее:

1. В корне проекта создайте файл `.env` (обратите внимание на точку в начале).
2. Вставьте в него строку (без пробелов вокруг `=` и без кавычек):

```
MENTORPIECE_API_KEY=mentorpiece_sim_ваш_длинный_ключ_здесь
```

3. Убедитесь, что файл `.gitignore` содержит строку `
.env
` — это предотвратит случайную отправку ключа в публичный репозиторий.

> Вместо реального ключа в репозитории храните шаблон в `.env.example` (уже добавлен).
