# 🚀 Quick Start Guide

## 5-Минутный Старт

### 1. Установка (1 мин)
```bash
pip install -r requirements.txt
```

### 2. Запуск тестов (30 сек)
```bash
pytest tests/ -v
```

**Ожидаемый результат:**
```
57 passed in 0.25s ✓
```

### 3. Проверка покрытия (30 сек)
```bash
pytest tests/ --cov=src --cov-report=term-missing
```

**Ожидаемый результат:**
```
TOTAL   70      2    97%
```

---

## 📚 Основные Команды

| Задача | Команда |
|--------|---------|
| Все тесты | `pytest tests/ -v` |
| Быстро | `pytest tests/ -q` |
| С покрытием | `pytest tests/ --cov=src` |
| Только критические | `pytest tests/ -m critical -v` |
| Только API тесты | `pytest tests/ -m api -v` |
| С HTML отчетом | `pytest tests/ --cov=src --cov-report=html` |

---

## 🎯 Что Тестируется?

- ✅ GET / (загрузка формы)
- ✅ POST / (обработка данных)
- ✅ Валидация входных данных
- ✅ Обработка ошибок (Network, HTTP 400/401/403/500/503)
- ✅ Интеграция с API (заголовки, payload, ответы)
- ✅ Безопасность API ключа
- ✅ Обработка markdown
- ✅ XSS protection
- ✅ Все 3 языка перевода

---

## 📊 Статистика

- **Всего тестов:** 57 ✓
- **Покрытие кода:** 97%
- **Время выполнения:** ~0.25 сек
- **Критические тесты:** 12
- **Тесты API:** 21

---

## 💡 Советы

**Перед коммитом:**
```bash
pytest tests/ -v
```

**Перед release:**
```bash
pytest tests/ --cov=src --cov-fail-under=95
```

**Для отладки с выводом логов:**
```bash
pytest tests/ -v -s
```

**Только быстрые тесты (без API):**
```bash
pytest tests/test_app.py -v
```

---

## ❓ FAQ

**Q: Все ли тесты проходят?**
A: Да, все 57 тестов успешно проходят (57 passed in 0.25s ✓)

**Q: Достаточное ли покрытие кода?**
A: Да, 97% покрытия (выше целевых 95%)

**Q: Как добавить новый тест?**
A: Смотрите комментарии в `tests/test_app.py` как примеры

**Q: Можно ли запустить конкретный тест?**
A: Да: `pytest tests/test_app.py::TestIndexGet::test_index_get_returns_200 -v`

**Q: Нужен ли реальный API для тестов?**
A: Нет, используются моки (mock объекты)

---

## 📖 Документация

1. **[TESTING.md](TESTING.md)** - Полная документация по тестам
2. **[REFACTORING.md](REFACTORING.md)** - Подробный отчёт о рефакторинге
3. **[OPTIMIZATION_METRICS.txt](OPTIMIZATION_METRICS.txt)** - Финальные метрики

---

## 🎓 Для Новичков

Все файлы содержат **МАКСИМАЛЬНО ПОДРОБНЫЕ КОММЕНТАРИИ** для обучения!

- `conftest.py` - Примеры фикстур
- `test_app.py` - Примеры функциональных тестов
- `test_api_integration.py` - Примеры тестов API

Начните с чтения комментариев в этих файлах.

---

**Версия:** 1.1  
**Дата:** 16 декабря 2025  
**Статус:** ✅ ГОТОВО
